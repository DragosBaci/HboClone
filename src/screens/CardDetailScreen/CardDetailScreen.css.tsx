import { styled } from "styled-components";
import { ScrollView } from "react-native";
import { Colors } from "../../utils/Colors";

export const CardDetailScreenContainer = styled(ScrollView)`
    background-color: ${Colors.black}
`;